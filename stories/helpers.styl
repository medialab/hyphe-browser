


button()
    border: none
    background: white
    font-family: var(--font-pt)
    cursor: pointer
    padding: 5px
    border-radius: var(--radius)

input()
    color: #aaa;
    max-width: 100%;
    text-overflow: ellipsis;
    outline: none
    border: none
    resize: none
    border-radius: var(--radius)
    padding: 5px
    font-size: 12px
    font-family: var(--font-mono)

fix()
    position: fixed
    left: 0
    top: 0
    width 100%
    height 100%

abs()
    position: absolute
    left: 0
    top: 0
    width 100%
    height 100%

stretch(mode)
    display: flex
    flex-flow: mode nowrap
    justify-content: stretch

flexCenter()
    display: flex
    flex-flow: row nowrap
    justify-content: center
    align-items: center
  