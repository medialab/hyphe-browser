.browser-tab-toolbar {
  background: var(--color-grey-light);
  padding-top: 8px;
}

// prev, next, reload
.tab-toolbar-navigation {
  flex-shrink: 1;
  flex-grow: 0;

  button {
    padding: 1em .5em;

    span {
      font-size: 16px;
    }
  }
}
