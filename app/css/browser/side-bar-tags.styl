// see https://github.com/JedWatson/react-select/blob/master/scss/control.scss

.browser-side-bar-tags {

  .browser-side-bar-tags-category {
    display: flex;
    margin-bottom: 10px;

    .category-name {
      font-family: var(--font-pt);
      font-size: 12px;
      font-weight: bold;
      text-align: left;
      margin-bottom: 8px;
      margin-top: 8px;
      padding-left: 11px;
      width: 130px;

      @media (max-width: 1024px) {
        width: 80px;
      }
    }
  }

  .browser-side-bar-tags-new-category {
    margin-top: 1em;

    // fake button when not focused
    input {
      background: var(--color-grey);
      border-color: var(--color-grey);
      cursor: text;
      padding-right: 30px;

      &::-webkit-input-placeholder {
        color: var(--color-black)
      }

      &:focus {
        background: var(--color-white);

        &::-webkit-input-placeholder {
          color: var(--color-white);
        }
      }
    }

    button {
      position: absolute;
      right: 20px;
      span {
        font-weight: bold;
      }
    }
  }

  .Select {

    .Select-control {
      background: var(--color-white);
      border: 1px solid var(--color-white);
      border-radius: var(--radius);
      box-shadow: none;
      text-align: left;
    }

    &.is-focused {
      .Select-control {
        border: 1px solid var(--color-black);
      }
    }

    .Select-input {
      div {
        white-space: pre-wrap;
        text-overflow: ellipsis;
      }
    }

    .Select-value {
      color: var(--color-black);
      font-family: var(--font-pt);
      font-size: 12px;
      text-align: left;
      cursor: text;
    }

    .Select-clear {
      margin-right: 4px;
      margin-top: 4px;
    }

    &.Select--single {
      .Select-control {
        width: 246px;
        @media (max-width: 1024px) {
          width: 185px;
        }
      }
      .Select-value {
        max-width: 231px;
        @media (max-width: 1024px) {
          max-width: 170px;
        }
      }
      .Select-input {
        width: 163px;
        @media (max-width: 1024px) {
          width: 170px;
        }
      }
    }
    // tag with the small cross before
    &.Select--multi {
      .Select-control {
        width: 376px;
        @media (max-width: 1024px) {
          width: 266px;
        }
      }
      .Select-input {
        width: 293px;
        @media (max-width: 1024px) {
          width: 300px;
        }
      }
      .Select-value {
        max-width: 361px;
        @media (max-width: 1024px) {
          max-width: 300px;
        }
        background: var(--color-grey-light);
        border: 0;
        margin: 7px 0px 0px 7px;
        font-weight: bold;

        .Select-value-icon {
          border: 0;

          &:hover {
            background: var(--color-grey);
            color: var(--color-black);
          }
        }
      }
    }

    .Select-arrow-zone {
      display: none;
    }

    .Select-menu-outer {
      background: var(--color-white);
      border-radius: var(--radius);
      border: 1px solid var(--color-grey-light);
      box-shadow: none;
      margin-top: 1px;
      z-index: 2;
      font-family: var(--font-pt);
    }

    .Select-menu {
      border-radius: var(--radius);
    }

    .Select-option {
      background: var(--color-white);
      padding-left: 12px;
      text-align: left;
      cursor: pointer;

      &.is-selected, &.is-focused, &:hover {
        background: var(--color-white);
        color: var(--color-blue);
      }
    }

    input:focus {
      border: none !important;
    }
  }
}

