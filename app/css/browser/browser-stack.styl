.browser-stack {
  display: flex;
  padding: 22px 2em 1em 2em;

  // left side

  progress,
  progress::-webkit-progress-bar {
    background: var(--color-grey);
    display: block;
    height: 4px;
    margin-top: .5em;
    width: 100%;
  }

  progress::-webkit-progress-value {
    background: var(--color-black);
  }

  // right colored buttons

  .fillers {
    display: flex;
    margin-left: 3em;
  }

  .filler {
    background: none;
    border: none;
    border-radius: 6px;
    padding: 0 1em;
    position: relative;
    text-align: left;

    &:hover {
      background: var(--color-grey-light);
    }

    &::after {
      background: var(--color-black);
      content: ''
      height: 24px;
      position: absolute;
      right: 0;
      top: 0;
      width: 1px;
    }

    &:last-child::after {
      display: none;
    }
  }

  .filler-name {
    cursor: pointer;
    font-family: var(--font-mont);
    font-size: 12px;
    font-weight: bold;
    padding: 3px;
  }

  .filler-counter {
    cursor: pointer;
    font-family: var(--font-pt);
    font-size: 11px;
    opacity: 0.75;
    padding-left: 6px;
  }

  .filler.selected .filler-name {
    background: var(--color-black);
    border-radius: 6px;
    color: var(--color-white);
  }

  .filler.selected .filler-counter {
    opacity: 1;
  }


  // see colors.styl

  .filler-UNDECIDED .filler-counter,
  .filler-IN_NOT_TAG .filler-counter,
  .filler-IN_NOT_CRAWLED .filler-counter {
    color: var(--color-corpus-undecided);
  }

  .filler-IN .filler-counter {
    color: var(--color-corpus-in);
  }

  .filler-OUT .filler-counter {
    color: var(--color-corpus-out);
  }

  .filler-PROSPECT .filler-counter {
    color: var(--color-corpus-discovered);
  }

}

.browser-stack-wes {
  display: flex;
}
  .browser-stack-wes-index {
    padding-right: .5em;
  }

  .browser-stack-wes-selector {
    flex-grow: 1;
    text-align: center;
  }

  .browser-stack-wes-selector .form-control {
    max-width: 500px;
    width: auto;
  }

  .browser-stack-wes-prev .icon {
  }

  .browser-stack-wes-next .icon {
    float: right;
  }

