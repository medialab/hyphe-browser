// used for loggin components

.pane-centered {
  margin: auto;
  width: 50vh;
}

.pane-centered-title {
  font-family: var(--font-mont);
  margin-top: 0;
  text-align: center;
}

.pane-centered-main {
  background: var(--color-grey);
  border-radius: var(--radius);
  padding: 1em;
}
