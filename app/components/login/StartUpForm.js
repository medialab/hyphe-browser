import React, { PropTypes } from 'react'

export default (props) => {
  return (
    <form className="start-up-form">
      <h2>Welcome to Hyphe</h2>
      <p>Please select a server instance:</p>
    </form>
  )
}
